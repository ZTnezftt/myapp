<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.DAO.InsertDataDAO">
    <insert id="insertDataToids">
        insert into ids values (#{id},#{title},#{imtro},#{albums})
    </insert>
</mapper>