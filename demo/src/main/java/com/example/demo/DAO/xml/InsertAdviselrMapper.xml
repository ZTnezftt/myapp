<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.DAO.InsertAdviselrDAO">

    <insert id="insertlr">
        replace into adviselr values (#{ids},#{type})
    </insert>

</mapper>